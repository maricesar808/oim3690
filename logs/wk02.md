# Week 2 Log

Date: 2026-05-25

> **What this is**: a short weekly summary of what you built, learned, and how you used AI. Not a diary — just structured evidence of your work.
>
> **Format**: use proper Markdown — headings, bullet points, code blocks. This file will be rendered on GitHub. If it looks like plain text with no formatting, you're doing it wrong.
>
> **Required**: all 4 sections present and non-empty. Empty sections = incomplete log.

## What I worked on

<!-- What did you build, fix, or explore this week? Link to repos or commits if relevant. -->

- I created an `about-me.html` page for my OIM 3690 folder.
- I practiced using semantic HTML tags like `<header>`, `<main>`, `<section>`, `<article>`, and `<footer>`.
- I added both a local image from my `images/` folder and an external image from a URL.
- I included a link, a list, headings, paragraphs, and descriptive `alt` text for images.

## What I learned

<!--
Concepts, tools, workflows, debugging tricks, code patterns — anything.
Use bullet points or ### sub-headings to organize by topic.
Include code snippets when relevant (but not required every week).
Explain in your own words.
-->

- I learned that semantic HTML helps organize a page by meaning, not just by appearance.
- I learned that `<header>` is usually the top part of a page, `<main>` holds the main content, and `<footer>` goes at the bottom.
- I learned that image `alt` text should describe what is in the image so the page is more accessible.
- I learned that heading order matters. The page should start with an `<h1>`, then use `<h2>` for main sections, and `<h3>` for smaller topics inside those sections.

## AI interactions

<!--
One block per interaction. Add more as needed.
-->

### Interaction 1

- **What I asked**: I asked AI to generate an `about-me.html` page that matched the assignment requirements from the screenshot.
- **What AI gave me**: AI created a full HTML page with a header, navigation, main content, a footer, a local image, an external image, a link, a list, and paragraphs.
- **What I kept / changed / rejected**: I kept the semantic structure and the clean layout. I reviewed the image paths, heading order, and `alt` text to make sure they matched the assignment. I changed the page content so it sounded like it was about me and not just a generic example.

## Questions

<!--
Things you're confused about, want to explore, or want help with.
As many as you have.
-->

- I want to keep practicing when to use `<section>` compared with `<article>`.
- I want to get more comfortable writing CSS without needing as much help.
